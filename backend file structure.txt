Notes/                          ← your existing React app (frontend)
├── src/
└── ...

Notes-server/                   ← new backend folder
├── models/
│   └── Note.js                 ← MongoDB schema
├── routes/
│   └── notes.js                ← CRUD routes
├── controllers/
│   └── notesController.js      ← route logic
├── middleware/
│   └── errorHandler.js         ← global error handler
├── .env                        ← MONGO_URI, PORT
├── server.js                   ← entry point
└── package.json